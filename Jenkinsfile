pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build Process'
            }
        }
        stage('Run') {
            steps {
                echo 'Run Process'
            }
        }
    }
}
